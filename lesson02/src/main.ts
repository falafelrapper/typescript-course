let myName: string;
let meaningOfLife: number;
let isLoading: boolean;
let album: any;

myName = 'Darlene'
meaningOfLife = 42
isLoading = true
album = 'Flower Boy'

const sum = (a: number , b: string) => {
    return a + b
}

let postId: string | number
let isActive: number | boolean
// Active status could be number and boolean also since booleans can sometimes be a value of 1 or 0

let re = /\w+/g